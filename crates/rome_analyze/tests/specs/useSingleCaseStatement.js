switch (foo) {
    case true:
    case false:
        let foo = '';
        foo;
}

switch (foo) {
    case true:
    case false:
        'yes';
}

switch (foo) {
    case true: {
        // empty
    }
}

switch (foo) {
    case true:
}
